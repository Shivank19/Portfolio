import React from 'react';

const ParallaxBackground: React.FC = () => {
  return (
    <div className="archive-page-bg fixed inset-0 pointer-events-none z-0" />
  );
};

export default ParallaxBackground;
